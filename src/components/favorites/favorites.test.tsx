describe('Function: getOffersByCity', () => {
  it('should return an object whose keys are city names and whose values are arrays of sentences in that city', () => {
    const value = 0;
    const result = 0;
    expect(result).toBe(value);
  });
});
